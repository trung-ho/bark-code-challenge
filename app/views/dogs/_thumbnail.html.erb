<a href="<%= url_for(dog) %>">
  <article>
    <h2 class="dog-name"><%= dog.name %></h2>
    <%= image_tag url_for(dog.images.first), class: "dog-photo", alt: "Photo of #{dog.name}" %>
  </article>
</a>
<% if ((dog_counter + 1) % 2 == 0) %>
  <%= image_tag image_path('ad.jpg')%>
<% end %>
